<!DOCTYPE html>
<html>
  <head>
    <title>CrediDiscount</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div class="container-flouid">
  <nav class="navbar navbar-expand-lg bg-light">
    <div class="container">
     <a class="navbar-brand" href="/">
        <h3>Credi Discount</h3>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
          <form class="">
              <% if user_signed_in? %> 
                <div> Welcome <%= current_user.name %> </div>  
                <%= button_to( "Log Out", destroy_user_session_path, method: :delete ) %>
                <%= button_to( "Log Out", destroy_user_session_path, method: :delete ) %>
              <% else %>
              <a class="btn btn-outline-primary mr-2" href="users/sign_in">Sign In</a>
              <a class="btn btn-outline-primary mr-2" href="users/sign_up">Register</a>
              <% end %>
          </form>
          </div>
        </div>
      </nav>
    </div>
      <div class="container-flouid">
      <div class="container">
        <%= yield %>
      </div>
    </div>
  </body>
</html>

